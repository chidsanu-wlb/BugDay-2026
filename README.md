# SCK Insurance - Mock Data Generator
## คู่มือการใช้งานสำหรับ QA Manual

---

## วัตถุประสงค์ของโปรเจ็ค

โปรเจ็คนี้เป็นการทดลองเขียน Prompt เพื่อให้ AI สร้าง Python Script สำหรับสร้างข้อมูลจำลอง (Mock Data) สำหรับการทดสอบระบบประกันครอบครัว (Family Bundle Insurance Renewal Campaign)

**เป้าหมาย:**
- ฝึกฝนการเขียน Prompt ให้ AI เพื่อสร้าง Python Script ที่สามารถสร้าง Test Data ที่มีความสมจริงใกล้เคียง Production ได้
- ใช้ AI ช่วยสร้างข้อมูลทดสอบที่ซับซ้อนตามเงื่อนไขทางธุรกิจ
- ลดเวลาในการสร้าง Test Data แบบ Manual

---

## โครงสร้างไฟล์ในโปรเจ็ค

```
BugDay-2026/
├── Business Requirement Document.md  # เอกสารความต้องการทางธุรกิจ (BRD)
├── family-condition.csv               # รายการเงื่อนไขครอบครัวที่ต้องทดสอบ
├── spec_v5.2.1.md                     # ตัวอย่าง Spec ที่เขียนให้ AI
├── generate-data-claude.py           # Python Script ที่สร้างจาก AI
├── SCK-insurance.csv                  # ไฟล์ Input สำหรับรัน Script
├── SCK_insurance_output_claude.csv    # ไฟล์ Output ตัวอย่างที่สร้างได้
└── README.md                          # ไฟล์นี้ (คู่มือการใช้งาน)
```

---

## ขั้นตอนการเริ่มต้น 

### 1. ติดตั้ง Python

**สำหรับ Windows:**
1. เข้าเว็บไซต์ https://www.python.org/downloads/
2. ดาวน์โหลด Python เวอร์ชันล่าสุด
3. รันไฟล์ติดตั้ง
4. **สำคัญ:** ติ๊กเลือก "Add Python to PATH" ก่อนกด Install
5. หลังติดตั้งเสร็จ เปิด Command Prompt แล้วพิมพ์:
   ```
   python --version
   ```
   ถ้าขึ้นเวอร์ชันแสดงว่าติดตั้งสำเร็จ

**สำหรับ Mac:**

**วิธีที่ 1: ดาวน์โหลดไฟล์ติดตั้ง**
1. เข้าเว็บไซต์ https://www.python.org/downloads/
2. ดาวน์โหลด Python เวอร์ชันล่าสุดสำหรับ macOS
3. รันไฟล์ติดตั้ง (.pkg)
4. หลังติดตั้งเสร็จ เปิด Terminal แล้วพิมพ์:
   ```
   python3 --version
   ```
   ถ้าขึ้นเวอร์ชันแสดงว่าติดตั้งสำเร็จ

**วิธีที่ 2: ใช้ Homebrew**
เปิด Terminal แล้วพิมพ์:
```bash
brew install python
```

### 2. ติดตั้ง Libraries ที่จำเป็น

**สำหรับ Windows:**
เปิด Command Prompt แล้วพิมพ์คำสั่งต่อไปนี้ทีละบรรทัด:

```bash
pip install pandas
pip install faker
pip install python-dateutil
```

**สำหรับ Mac:**
เปิด Terminal แล้วพิมพ์คำสั่งต่อไปนี้ทีละบรรทัด:

```bash
pip3 install pandas
pip3 install faker
pip3 install python-dateutil
```

---

## ขั้นตอนการใช้งาน

### ขั้นตอนที่ 0: เตรียมข้อมูลตัวอย่างด้วยตนเอง (Manual Sample)

**สำคัญ:** ก่อนใช้ AI ช่วยสร้างข้อมูล ควรเตรียมข้อมูลตัวอย่างด้วยตนเอง 1 record เพื่อให้ได้ข้อมูลที่ถูกต้องและเป็นมาตรฐาน

**ขั้นตอน:**
1. เลือกเงื่อนไขครอบครัว 1 กรณีจาก `family-condition.csv`
2. เตรียมข้อมูลตัวอย่างด้วยตนเองโดยคำนวณ:
   - วันเกิดที่เหมาะสมตามเงื่อนไขอายุ
   - อายุที่คำนวณถูกต้องตามวันที่อ้างอิง
   - วันที่ต่ออายุและวันสิ้นสุดความคุ้มครอง
3. ตรวจสอบความถูกต้องของข้อมูล
4. ใช้ข้อมูลตัวอย่างนี้เป็น Reference เมื่อเขียน Prompt ให้ AI

**ตัวอย่างข้อมูลตัวอย่างที่เตรียมด้วยตนเอง:**

| ผู้เอาประกัน | สามาชิกในครอบครัว | ข้อมูลกรรมธรรม์ |
|---------------|-------------------|------------------|
| ชื่อ: จันทภา ถาวรรัตน<br>เลขบัตรประชาชน: 9078428428461<br>เบอร์โทรศัพท์: 0885430850<br>อีเมล: john@email.com<br>ว/ด/ป เกิด: 2 มกราคม 2515<br>อายุ: 54 ปี 253 วัน | คู่สมรส: มีชีวิต (อายุเกิน)<br>ชื่อ: ฌาฆีภัตฐ์ ประจันตะเสน<br>ว/ด/ป เกิด: 25 สิงหาคม 2504<br>อายุ: 65 ปี 59 วัน<br><br>บุตรคนที่ 1: มีชีวิต (อายุเกิน)<br>ชื่อ: คมคาย บุญญาภิรมย์<br>ว/ด/ป เกิด: 27 กรกฎาคม 2554<br>อายุ: 15 ปี 88 วัน<br><br>บุตรคนที่ 2: มีชีวิต (อายุเกิน)<br>ชื่อ: สุธินันท์ แน่นดุจป้อม<br>ว/ด/ป เกิด: 14 ธันวาคม 2553<br>อายุ: 15 ปี 313 วัน | จำนวนวันที่ต่ออายุล่วงหน้า: 41 วัน<br>วันที่ต่ออายุ: 12 กันยายน 2569<br>วันสิ้นสุดความคุ้มครอง: 23 ตุลาคม 2569<br>วันเริ่มสัญญาฉบับปัจจุบัน: 23 ตุลาคม 2568 |

### ขั้นตอนที่ 1: เตรียมไฟล์ Input

ไฟล์ `SCK-insurance.csv` คือไฟล์ Input ที่ระบุเงื่อนไขการทดสอบ ประกอบด้วย Columns:
- `Timeline`: ระยะเวลาที่ต่ออายุล่วงหน้า (เช่น "90 - 30 วันล่วงหน้า")
- `Bundle`: ประเภทแพกเกจ (เช่น "SCK Family", "SCK Personal")
- `Family`: เงื่อนไขครอบครัว (ต้องตรงกับที่อยู่ใน `family-condition.csv`)

### ขั้นตอนที่ 2: เขียน Prompt ให้ AI

ต้องทำการเขียน Prompt ให้ AI เพื่อให้สร้าง Python Script โดยอ้างอิงจาก:

1. **Business Requirement Document.md** - อธิบายเงื่อนไขทางธุรกิจ
2. **family-condition.csv** - รายการเงื่อนไขครอบครัวทั้งหมด

**ตัวอย่าง Prompt:**
```
เขียน Python Script สำหรับสร้าง Mock Data สำหรับระบบประกันครอบครัว
โดยอ่านไฟล์ Input CSV และสร้าง Output CSV ตามเงื่อนไขใน Business Requirement Document.md
และ family-condition.csv

เงื่อนไขหลัก:
- วันที่ต่ออายุ (RENEWAL_DATE): 2026-09-12
- คำนวณอายุตามปฏิทินจริง
- ผู้ใหญ่อายุไม่เกิน 65 ปีบริบูรณ์
- เด็กอายุไม่เกิน 15 ปีบริบูรณ์
- ใช้ faker library สร้างชื่อไทยและข้อมูลสุ่ม
- บันทึกไฟล์ด้วย encoding utf-8-sig
```

### ขั้นตอนที่ 3: รัน Python Script

1. บันทึก Python Script ที่ AI สร้างให้ (เช่นชื่อ `generate-data.py`)
2. เปิด Command Prompt/Terminal ในโฟลเดอร์โปรเจ็ค
3. รัน Script ด้วยคำสั่ง:

   **สำหรับ Windows:**
   ```bash
   python generate-data.py
   ```

   **สำหรับ Mac:**
   ```bash
   python3 generate-data.py
   ```

4. Script จะสร้างไฟล์ Output CSV ให้

### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์

เปิดไฟล์ Output CSV ด้วย Excel แล้วตรวจสอบ:
- ข้อมูลผู้ถือกรมธรรม์ถูกต้อง
- ข้อมูลสมาชิกในครอบครัวตรงตามเงื่อนไข
- อายุคำนวณถูกต้อง
- วันที่อยู่ในรูปแบบไทย

---

## ข้อควรระวัง

### 1. การคำนวณอายุ

**หมายเหตุ:** Spec ระบุให้ใช้ `dateutil.relativedelta` สำหรับคำนวณอายุตามปฏิทินจริง เพื่อรองรับปีอธิกสุรทิน (Leap Year) และให้ความแม่นยำตามหลัก Business Logic

**สิ่งที่ต้องตรวจสอบ:**
- ตรวจสอบว่า AI ใช้ `dateutil.relativedelta` ตามที่ระบุใน spec
- ตรวจสอบว่าอายุคำนวณถูกต้องตามวันเกิดและวันที่อ้างอิง
- บาง AI Model อาจไม่ทำตาม spec ครบถ้วน ควรตรวจสอบ Output ด้วยตนเอง

### 2. ความสมจริงของข้อมูลครอบครัว

**ปัญหา:** ใน Spec เวอร์ชันปัจจุบันยังไม่ได้รองรับการสร้างข้อมูลที่มีความสมจริงเชิงลึก

**ตัวอย่างที่ควรปรับปรุง:**
- **อายุบุตร:** ถ้ามีบุตร 2 คน ควรห่างกันประมาณ 9 เดือนขึ้นไปเพื่อความสมจริง (เพราะไม่สามารถคลอดห่างกันน้อยกว่านั้น)
- **วันเกิดคู่สมรส:** ควรมีความเป็นไปได้ที่สมจริง (ไม่ห่างกันมากเกินไปหรือน้อยเกินไป)
- **ชื่อ-นามสกุล:** ควรมีความสัมพันธ์กันระหว่างสมาชิกในครอบครัว

**การปรับปรุง:**
เพิ่มเงื่อนไขใน Prompt ให้ AI คำนึงถึงความสมจริงเหล่านี้:
```
- ถ้ามีบุตร 2 คน ให้วันเกิดห่างกันอย่างน้อย 9 เดือน
- คู่สมรสควรมีอายุห่างกันไม่เกิน 20 ปี
- ให้บุตรใช้นามสกุลเดียวกับผู้ถือกรมธรรม์
```

### 4. การตรวจสอบข้อมูล

หลังจาก AI สร้าง Script แล้ว ควร:
1. รัน Script และตรวจสอบ Output อย่างน้อย 5-10 รายการ
2. เปรียบเทียบกับตัวอย่างใน `SCK_insurance_output_claude.csv`
3. ตรวจสอบ edge cases (กรณีพิเศษ) เช่น:
   - คนที่อายุเกินเกณฑ์
   - คนที่เสียชีวิต
   - กรณีที่ไม่มีคู่สมรส

---

## ตัวอย่างไฟล์ Output

ไฟล์ Output จะมี 6 Columns:

| Column | คำอธิบาย |
|--------|-----------|
| Timeline | ระยะเวลาที่ต่ออายุล่วงหน้า |
| Bundle | ประเภทแพกเกจ |
| Family | เงื่อนไขครอบครัว |
| ข้อมูลผู้ถือกรมธรรม์ | ชื่อ, เลขบัตรประชาชน, เบอร์โทร, อีเมล, วันเกิด, อายุ |
| ข้อมูลสมาชิกในกรมธรรม์ | รายละเอียดคู่สมรสและบุตร (ถ้ามี) |
| Status | วันที่ต่ออายุ, วันสิ้นสุดความคุ้มครอง, วันเริ่มสัญญา |

---

## Output Format ที่ต้องการ (สำหรับ Database Schema)

**หมายเหตุ:** แม้ว่าในโจทย์ปัจจุบันยังไม่ได้คิดเรื่องเก็บลง Database แต่ควรมี Output Format ที่ชัดเจนเพื่อใช้เป็น Reference สำหรับการออกแบบ Database Schema ในภายหลัง

### โครงสร้างข้อมูลที่ต้องการ:

#### 1. ข้อมูลผู้เอาประกัน (Insured Person)
| Field | Data Type | Format | คำอธิบาย |
|-------|-----------|--------|-----------|
| ชื่อ | VARCHAR(100) | "จันทภา ถาวรรัตน" | ชื่อ-นามสกุลภาษาไทย |
| เลขบัตรประชาชน | VARCHAR(13) | "9078428428461" | เลขบัตรประชาชน 13 หลัก |
| เบอร์โทรศัพท์ | VARCHAR(10) | "0885430850" | เบอร์โทรศัพท์มือถือ |
| อีเมล | VARCHAR(100) | "john@email.com" | อีเมล |
| วันเกิด | DATE | "1972-01-02" | วันเกิด (ISO format) |
| อายุปี | INT | 54 | อายุเป็นปี |
| อายุวัน | INT | 253 | อายุเป็นวัน |

#### 2. ข้อมูลสมาชิกในครอบครัว (Family Members)
| Field | Data Type | Format | คำอธิบาย |
|-------|-----------|--------|-----------|
| ประเภทสมาชิก | VARCHAR(20) | "คู่สมรส", "บุตรคนที่ 1", "บุตรคนที่ 2" | ประเภทสมาชิก |
| สถานะชีวิต | VARCHAR(20) | "มีชีวิต", "เสียชีวิต" | สถานะชีวิต |
| สถานะอายุ | VARCHAR(20) | "อายุเกิน", "อายุไม่เกิน" | สถานะอายุตามเกณฑ์ |
| ชื่อ | VARCHAR(100) | "ฌาฆีภัตฐ์ ประจันตะเสน" | ชื่อ-นามสกุล |
| วันเกิด | DATE | "1961-08-25" | วันเกิด (ISO format) |
| อายุปี | INT | 65 | อายุเป็นปี |
| อายุวัน | INT | 59 | อายุเป็นวัน |

#### 3. ข้อมูลกรรมธรรม์ (Policy Information)
| Field | Data Type | Format | คำอธิบาย |
|-------|-----------|--------|-----------|
| จำนวนวันที่ต่ออายุล่วงหน้า | INT | 41 | จำนวนวัน |
| วันที่ต่ออายุ | DATE | "2026-09-12" | วันที่ต่ออายุ (ISO format) |
| วันสิ้นสุดความคุ้มครอง | DATE | "2026-10-23" | วันสิ้นสุดความคุ้มครอง (ISO format) |
| วันเริ่มสัญญาฉบับปัจจุบัน | DATE | "2025-10-23" | วันเริ่มสัญญา (ISO format) |

#### 4. ข้อมูลทดสอบ (Test Metadata)
| Field | Data Type | Format | คำอธิบาย |
|-------|-----------|--------|-----------|
| Timeline | VARCHAR(50) | "90 - 30 วันล่วงหน้า" | ระยะเวลาที่ต่ออายุล่วงหน้า |
| Bundle | VARCHAR(50) | "SCK Family" | ประเภทแพกเกจ |
| Family | VARCHAR(100) | "มีคู่สมรส + บุตร 2 คน (ทั้งหมดอายุเกิน)" | เงื่อนไขครอบครัว |

**หมายเหตุเพิ่มเติม:**
- วันที่ใน Database ควรเก็บเป็น ISO format (YYYY-MM-DD) เพื่อความเป็นมาตรฐาน
- แต่ในไฟล์ Output CSV ควรแสดงเป็นภาษาไทย (เช่น "2 มกราคม 2515") เพื่อความเข้าใจง่าย
- เลขบัตรประชาชนควรตรวจสอบความถูกต้อง 13 หลัก
- เบอร์โทรศัพท์ควรเป็น 10 หลัก (ขึ้นต้นด้วย 08, 09, 06)

---

## การแก้ไขปัญหา

### ปัญหา: ไฟล์ Output เปิดใน Excel แล้วภาษาไทยไม่ขึ้น

**วิธีแก้:** ตรวจสอบว่า Script บันทึกไฟล์ด้วย `encoding="utf-8-sig"`

### ปัญหา: อายุคำนวณไม่ถูกต้อง

**วิธีแก้:** ตรวจสอบว่าใช้ `relativedelta` จาก library `dateutil` และไม่ใช้การคำนวณแบบง่ายๆ

---

## แหล่งอ้างอิง

- **Business Requirement Document.md** - เอกสารความต้องการทางธุรกิจ
- **spec_v5.2.1.md** - ตัวอย่าง Spec ที่เขียนดี
- **generate-data-claude.py** - ตัวอย่าง Python Script ที่ทำงานได้
- **SCK_insurance_output_claude.csv** - ตัวอย่าง Output ที่ถูกต้อง

---

## Note

1. **เริ่มจาก Prompt ที่ชัดเจน:**
2. **ทดลองกับ AI หลายตัว:** เปรียบเทียบผลลัพธ์จาก Claude, GPT-4, และ Copilot
3. **บันทึก Prompt ที่ดี:** เก็บ Prompt ที่ให้ผลลัพธ์ดีไว้ใช้ซ้ำ
4. **ตรวจสอบเสมอ:** อย่าไว้ใจ AI 100% ต้องตรวจสอบ Output ด้วยตนเอง

